<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textView"
        style="@style/Base.TextAppearance.AppCompat.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Invest Dream"
        android:textSize="20dp" />

    <Button
        android:id="@+id/btCalculadora"
        android:layout_width="400dp"
        android:layout_height="70dp"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:text="Calculadora de Contratos" />

    <Button
        android:id="@+id/btRegistroDeInteressados"
        android:layout_width="400dp"
        android:layout_height="70dp"
        android:layout_alignLeft="@+id/btCalculadora"
        android:layout_alignStart="@+id/btCalculadora"
        android:layout_below="@+id/btCalculadora"
        android:text="Registro de Interessados" />

    <Button
        android:id="@+id/btContato"
        android:layout_width="400dp"
        android:layout_height="70dp"
        android:layout_alignLeft="@+id/btCalculadora"
        android:layout_alignStart="@+id/btCalculadora"
        android:layout_below="@+id/btRegistroDeInteressados"
        android:enabled="false"
        android:text="Contato (Em breve...)" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:src="@drawable/logosplash" />


</RelativeLayout>
